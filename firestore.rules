rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Require authentication for all reads and writes by default
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
